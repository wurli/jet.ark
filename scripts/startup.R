# These options make a pretty big difference to how things are printed in the
# console, especially if you're a tidyverse user.
options(cli.default_num_colors = 256L)
options(cli.dynamic = TRUE)
options(cli.ansi = TRUE)
options(cli.hyperlink = TRUE)
